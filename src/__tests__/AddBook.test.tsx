import React from 'react';
import { render, fireEvent } from '@testing-library/react';

import AddBook, { Props } from '../components/AddBook';

describe('<AddBook />', () => {
  test('Should display blank form', async () => {
    // Do stuff
  });
});
